<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <a class="navbar-brand" href="#">
        <img alt="Brand" src="/assets/coffee_buddy_logo.png" height="20px">
      </a>
      <%= link_to "Coffee Buddy", root_path, class: "navbar-brand"%>
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">

        <li class="active"><%= link_to "Home", root_path%></li>
        <li><%= link_to "Cafes", shops_path%></li>
        <li><%= link_to "About Coffee", products_path%></li>
      </ul>
      <form class="navbar-form navbar-left" role="search">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="Get cafes by address">
        </div>
        <button type="submit" class="btn btn-primary">Cafes nearby</button>
      </form>
      <% if current_user && current_user.is_admin?%>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "Hi, #{current_user.name}", user_path(current_user.id) %></li>
        <li role="presentation">
          <%= link_to '#', :class => 'button white' do %>
              Message
              <span class="badge">
                <%= current_user.meetings.where('readed' => true).count%>
              </span>
          <% end %>
        </li>
        <li><%= link_to "Admin", casein_root_path %></li>
        <li><%= link_to "Logout", logout_path, :method => :delete %></li>
      </ul>
      <% elsif current_user%>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "Hi, #{current_user.name}", user_path(current_user.id) %></li>
        <li role="presentation">
          <%= link_to '#', :class => 'button white' do %>
              Message
              <span class="badge">
                <%= current_user.meetings.where('readed' => true).count%>
              </span>
          <% end %>
        </li>
        <li><%= link_to "Logout", logout_path, :method => :delete %></li>
      </ul>
      <% else %>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "Sign Up", new_user_path %><li>
        <li><%= link_to "Login", login_path %><li>
      </ul>
      <% end %>

    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
